<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Your To-Do List</title>
</head>
<body>

    <div class="container">

        <h1>Your To-Do List</h1>
        
        <ul class="todo-list">
            <% for(var i=0; i < tasks.length; i++){ %>
                <li class="todo-item">
                    <input type="checkbox" class="checkbox">
                    <label class="task-label"><%= tasks[i] %></label>
                    <form action="/delete" method="post" class="delete-form">
                        <input type="hidden" name="todoTask" value="<%= tasks[i] %>">
                        <button class="delete-btn" type="submit">X</button>
                    </form>
                </li>
            <% } %>
        </ul>
        
        <form action="/add" method="post" class="add-form">
            <input type="text" name="todos" placeholder="Add new thing" class="add-input">
            <button type="submit" class="add-btn">Add Thing</button>
        </form>

    </div>

</body>
</html>

